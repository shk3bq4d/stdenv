#!/usr/bin/env bash
# ex: set filetype=sh :

set -euo pipefail

GREP_ARGS=
for var in "$@"
do
	[[ -f "$var" ]] && break
	GREP_ARGS="$GREP_ARGS $var"
	shift
done


#echo "GREP_ARGS is $GREP_ARGS"
#echo "files are $@"

~/py/yaml-paths.py $@ | grep -iE --color=always $GREP_ARGS

exit 0
